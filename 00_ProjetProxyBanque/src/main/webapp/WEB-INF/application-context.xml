<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd
		
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.1.xsd">
		<!--  Sert à configurer le Context Spring Core -->
	<!-- Activation de la detection des annotations -->
	<context:annotation-config/>
	
	<!-- Activation de la detection des compo -->
	<context:component-scan base-package="fr"/>
	
	<!-- Localisation du fichier jdbc.properties -->
	<context:property-placeholder location="classpath:jdbc.properties"/>
	
	<!-- Declaration du bean DataSource -->
	<bean id="dataSourceBean" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="${jdbc.driverClassName}"/>
		<property name="url" value="${jdbc.url}"/>
		<property name="username" value="${jdbc.username}"></property>
		<property name="password" value="${jdbc.password}"></property>
	</bean>
	
	<!-- Declaration du bean SessionFactory -->
	<bean name="sessionFactoryBean" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
		
		<!--  Injection du bean dataSource -->
		<property name="dataSource" ref="dataSourceBean"></property>
		
		<!-- Les classes avec la annotations JPA -->
		<property name="annotatedClasses" value="fr.adaming.model.Employe"/>
		
		<!-- Les propriétés HIbernate -->
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">${jdbc.hibernate.dialect}</prop>
				<prop key="hibernate.show_sql">${jdbc.hibernate.show_sql}</prop>
				<prop key="hibernate.hbm2ddl.auto">${jdbc.hibernate.hbm2ddl.auto}</prop>
			</props>
		</property>
		
	</bean>
	
	<!--  Declaration du bean transaction Manager -->
	<bean id="transactionManagerBean" class="org.springframework.orm.hibernate4.HibernateTransactionManager">
		<!-- Injection du bean -->
		<property name="sessionFactory" ref="sessionFactoryBean"/>
	</bean>

</beans>
